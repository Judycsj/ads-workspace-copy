<!-- ads-workspace-gdoc-sync: gdoc_id=1jh3ULCxBn2o2LlLGP2ZTL7f2AFdp5xvHW36uY4n_Qvg gdoc_url=https://docs.google.com/document/d/1jh3ULCxBn2o2LlLGP2ZTL7f2AFdp5xvHW36uY4n_Qvg/edit -->

# SQL Patterns: mp_mgmt.dws_order_item_rebate_di__reg_s0_live

*Auto-generated by ads-kb-datamap (from-code) -- 41 references (0 write, 41 read)*
*Generated on: 2026-06-16*

## Common WHERE Filters

**标准读模式** (所有 33 个 workflow 文件 + 8 个 playground):
```sql
where
    grass_date between date('${grass_date_30day}') and date('${grass_date}')
    and grass_region = upper('${region}')
    and is_bi_excluded_rev_prm = 0
```

**Regional variant** (dws_user_pc2_1d `_regional` 后缀, 如 `dws_user_pc2_1d__id_regional`):
```sql
where grass_region = upper('${region}')
and grass_date between date_add(date('${grass_date_30day}'), -1) and date('${grass_date}')
and regional_create_date >= date('${grass_date_30day}')
and regional_create_date < date_add(date('${grass_date}'), 1)
and is_bi_excluded_rev_prm = 0
```

## Common JOIN Patterns

JOIN on 6-column key (与 `dws_order_item_rev_di` 和 `traffic_omni_oa.dwd_order_item_atc_journey_di` 对齐):
```sql
left join rebate_base
    on omni.order_id = rebate.order_id
    and omni.item_id = rebate.item_id
    and omni.model_id = rebate.model_id
    and omni.group_id = rebate.group_id
    and omni.bundle_order_item_id = rebate.bundle_order_item_id
    and omni.grass_date = rebate.grass_date
```

## Common Aggregations

**rebate_base** (from dws_order_item_rebate_di, 2 个 workflow 中 GROUP BY 到不同粒度):
```sql
-- Aggregate to order-item-model-group-bundle level
select
    grass_date
    ,order_id
    ,item_id
    ,model_id
    ,group_id
    ,bundle_order_item_id
    ,coalesce(estimate_net_total_item_voucher_logst_prm_usd_level1, 0) as estimate_net__total_item_voucher_logst_prm_usd_level1
    ,coalesce(estimate_net_total_logst_prm_usd_level1, 0) as estimate_net__total_logst_prm_usd_level1
    ,coalesce(estimate_net_total_item_card_prm_usd_level1, 0) as estimate_net__total_item_card_prm_usd_level1
    ,coalesce(estimate_net_total_voucher_prm_usd_level1, 0) as estimate_net__total_voucher_prm_usd_level1
    ,coalesce(estimate_net_total_coin_prm_usd_level1, 0) as estimate_net__total_coin_prm_usd_level1
from mp_mgmt.dws_order_item_rebate_di__reg_s0_live
```

## Representative SQL Snippets

### Snippet 1: Standard rebate_base view (workflows/dws_user_pc2_1d and dws_common_feature_user_item_pc2_1d)
```sql
create or replace temporary view rebate_base as
select
    grass_date
    ,order_id
    ,item_id
    ,model_id
    ,group_id
    ,bundle_order_item_id
    ,coalesce(estimate_net_total_item_voucher_logst_prm_usd_level1, 0) as estimate_net__total_item_voucher_logst_prm_usd_level1 -- PRM excl. 3PL Margin
    ,coalesce(estimate_net_total_logst_prm_usd_level1, 0) as estimate_net__total_logst_prm_usd_level1 -- Logistic PRM
    ,coalesce(estimate_net_total_item_card_prm_usd_level1, 0) as estimate_net__total_item_card_prm_usd_level1 -- Item PRM
    ,coalesce(estimate_net_total_voucher_prm_usd_level1, 0) as estimate_net__total_voucher_prm_usd_level1 -- Voucher PRM
    ,coalesce(estimate_net_total_coin_prm_usd_level1, 0) as estimate_net__total_coin_prm_usd_level1 -- Coin PRM
from mp_mgmt.dws_order_item_rebate_di__reg_s0_live
where
    grass_date between date('${grass_date_30day}') and date('${grass_date}')
    and grass_region = upper('${region}')
    and is_bi_excluded_rev_prm = 0
;
```

### Snippet 2: Regional variant with regional_create_date (dws_user_pc2_1d__id_regional, etc.)
```sql
create or replace temporary view rebate_base as
select
    regional_create_date as grass_date
    ,order_id
    ,item_id
    ,model_id
    ,group_id
    ,bundle_order_item_id
    ,coalesce(estimate_net_total_item_voucher_logst_prm_usd_level1, 0) as estimate_net__total_item_voucher_logst_prm_usd_level1 -- PRM excl. 3PL Margin
    ,coalesce(estimate_net_total_logst_prm_usd_level1, 0) as estimate_net__total_logst_prm_usd_level1 -- Logistic PRM
    ,coalesce(estimate_net_total_item_card_prm_usd_level1, 0) as estimate_net__total_item_card_prm_usd_level1 -- Item PRM
    ,coalesce(estimate_net_total_voucher_prm_usd_level1, 0) as estimate_net__total_voucher_prm_usd_level1 -- Voucher PRM
    ,coalesce(estimate_net_total_coin_prm_usd_level1, 0) as estimate_net__total_coin_prm_usd_level1 -- Coin PRM
from mp_mgmt.dws_order_item_rebate_di__reg_s0_live
where grass_region = upper('${region}')
and grass_date between date_add(date('${grass_date_30day}'), -1) and date('${grass_date}')
and regional_create_date >= date('${grass_date_30day}')
and regional_create_date < date_add(date('${grass_date}'), 1)
and is_bi_excluded_rev_prm = 0
;
```

### Snippet 3: Playground validation query
```sql
select 'mp_rebate', null, sum(estimate_net__total_item_voucher_logst_prm_usd_level1)
from  mp_mgmt.dws_order_item_rebate_di__reg_s0_live
where grass_region='SG'  and grass_date=date'2025-12-01' and is_bi_excluded_rev_prm = 0
;
```

## Write Lineage (生产血缘)

未在代码库中找到 INSERT OVERWRITE / CREATE TABLE 生产逻辑。该表的写入逻辑可能在其他代码库或 DataStudio 中直接管理。

## Derived Metrics (派生指标)

未识别到跨 2+ 文件重复出现的派生指标公式（该表字段在上游已计算好，下游直接 coalesce 后使用）。
