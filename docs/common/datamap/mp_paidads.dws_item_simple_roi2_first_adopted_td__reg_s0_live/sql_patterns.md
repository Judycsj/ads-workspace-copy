<!-- ads-workspace-gdoc-sync: gdoc_id=1-Siod2e-njhmiaTo--RMB7ongSMq-QobgCoxiYY3wzM gdoc_url=https://docs.google.com/document/d/1-Siod2e-njhmiaTo--RMB7ongSMq-QobgCoxiYY3wzM/edit -->

# SQL Patterns: mp_paidads.dws_item_simple_roi2_first_adopted_td__reg_s0_live

*Auto-generated by ads-kb-datamap (from-code) -- 9 references (8 write, 1 read)*
*Generated on: 2026-06-16*

## Common WHERE Filters

```sql
-- 标准分区过滤（所有查询）
WHERE tz_type = 'local'
  AND grass_date = date('${BIZ_YESTERDAY}')
  AND grass_region = '${upper_region}'

-- 读场景：指定日期的首次采纳商品
WHERE grass_date = ${L1D_Date}
  AND item_first_adopted_date BETWEEN ${L1D_Date} - INTERVAL '7' DAY AND ${L1D_Date}

-- 读场景：前一天全量
WHERE grass_date = date('${PREV_2D}')
  AND item_id > 0
```

## Common JOIN Patterns

```sql
-- JOIN dim_item 补全商品属性（写场景）
LEFT OUTER JOIN (
    SELECT DISTINCT item_id, shop_id, seller_id,
        level1_global_be_category_id, level1_global_be_category,
        level2_global_be_category_id, level2_global_be_category,
        level3_global_be_category_id, level3_global_be_category
    FROM mp_item.dim_item__reg_s0_live
    WHERE tz_type = 'local'
      AND grass_date = date('${BIZ_YESTERDAY}')
      AND grass_region = '${upper_region}'
) b ON a.item_id = b.item_id

-- JOIN 自身获取采纳日期（读场景：before-after 对比）
JOIN dim_adopt d
  ON a.item_id = d.item_id
 AND a.grass_region = d.grass_region
 AND a.grass_date = d.grass_date
```

## Common Aggregations

```sql
-- 去重取首次采纳日期（写场景）
SELECT item_id,
       MIN(item_first_adopted_date) AS item_first_adopted_date,
       MAX(is_migrated_item) AS is_migrated_item
FROM ...
GROUP BY item_id

-- 读场景：按商品+区域+采纳日期分组
GROUP BY 1, 2, 3, 4
```

## Representative SQL Snippets

### 1. Write: 每日全量更新（INSERT OVERWRITE，自 UNION 前一天数据）

```sql
INSERT OVERWRITE TABLE dws_item_simple_roi2_first_adopted_td__reg_s0_live
PARTITION (tz_type = 'local', grass_region, grass_date)
SELECT
    a.item_id, shop_id, seller_id,
    level1_global_be_category_id, level1_global_be_category,
    level2_global_be_category_id, level2_global_be_category,
    level3_global_be_category_id, level3_global_be_category,
    item_first_adopted_date,
    COALESCE(is_migrated_item, 0) AS is_migrated_item,
    '${upper_region}' AS grass_region,
    date('${BIZ_YESTERDAY}') AS grass_date
FROM (
    SELECT item_id,
           MIN(item_first_adopted_date) AS item_first_adopted_date,
           MAX(is_migrated_item) AS is_migrated_item
    FROM (
        -- 今日新采纳的商品
        SELECT a.item_id, date('${BIZ_YESTERDAY}') AS item_first_adopted_date, is_migrated_item
        FROM (
            SELECT item_id, date('${BIZ_YESTERDAY}') AS item_first_adopted_date
            FROM mp_paidads.ads_advertise_mkt_1d__reg_s0_live
            WHERE tz_type = 'local'
              AND grass_date = date('${BIZ_YESTERDAY}')
              AND grass_region = '${upper_region}'
              AND placement = 50
              AND item_id > 0
              AND impression_cnt > 0
            GROUP BY item_id
        ) a
        LEFT OUTER JOIN (
            SELECT item_id, 1 AS is_migrated_item
            FROM mp_paidads.ads_advertise_mkt_1d__reg_s0_live
            WHERE tz_type = 'local'
              AND grass_date = date('${BIZ_YESTERDAY}')
              AND grass_region = '${upper_region}'
              AND item_id > 0
              AND (is_ads_active = 1 OR impression_cnt > 0)
              AND (pricing_type = 3
                   OR (pricing_type IN (4, 8) AND placement NOT IN (20, 2003, 2030)))
            GROUP BY item_id
        ) b ON a.item_id = b.item_id

        UNION ALL

        -- 昨天的存量商品（保持原有采纳日期）
        SELECT item_id, item_first_adopted_date, is_migrated_item
        FROM mp_paidads.dws_item_simple_roi2_first_adopted_td__reg_s0_live
        WHERE tz_type = 'local'
          AND grass_region = '${upper_region}'
          AND grass_date = date('${PREV_2D}')
          AND item_id > 0
    ) t
    GROUP BY item_id
) a
LEFT OUTER JOIN (
    SELECT DISTINCT item_id, shop_id, seller_id, ...
    FROM mp_item.dim_item__reg_s0_live
    WHERE tz_type = 'local'
      AND grass_date = date('${BIZ_YESTERDAY}')
      AND grass_region = '${upper_region}'
) b ON a.item_id = b.item_id
```

### 2. Read: 获取近期首次采纳商品

```sql
WITH dim_adopt AS (
    SELECT item_id, shop_id, grass_region,
           item_first_adopted_date, grass_date
    FROM mp_paidads.dws_item_simple_roi2_first_adopted_td__reg_s0_live
    WHERE grass_date = ${L1D_Date}
      AND item_first_adopted_date BETWEEN ${L1D_Date} - INTERVAL '7' DAY AND ${L1D_Date}
    GROUP BY 1, 2, 3, 4, 5
)
```

## Write Lineage (生产血缘)

### 生产流程

| 项目 | 内容 |
|------|------|
| Workflow 路径 | `studio_tasks/data_paidadsmart/workflows/data_warehouse/dws/roi2/dws_item_simple_roi2_first_adopted_td__reg_s0_live/` |
| 文件数 | 8 (每个 region 一个: vn/id/th/ph/my/mx/br/tw) |
| 写入方式 | INSERT OVERWRITE ... PARTITION (tz_type = 'local', grass_region, grass_date) |
| 调度频率 | Daily (${BIZ_YESTERDAY}) |
| 分区策略 | 每日全量覆盖当天分区 (union all 前一天全量 + 今日新增) |

### 上游表

| 上游表 | 用途 | JOIN 条件 |
|--------|------|-----------|
| mp_paidads.ads_advertise_mkt_1d__reg_s0_live | 识别今日新采纳 Simple2 的商品 (placement=50, impression>0) | 自 JOIN |
| mp_paidads.ads_advertise_mkt_1d__reg_s0_live | 识别迁移商品 (pricing_type=3 或 Simple 自动) | LEFT JOIN ON item_id |
| mp_paidads.dws_item_simple_roi2_first_adopted_td__reg_s0_live | 自引用: 前一天分区数据 (保持历史采纳日期) | UNION ALL |
| mp_item.dim_item__reg_s0_live | 补充商品基本属性 (shop/seller/category) | LEFT JOIN ON item_id |

### 下游引用

| 下游表/SQL | 引用方式 |
|------------|----------|
| manual_tasks/shijing/simple2_before_after | 作为 dim_adopt CTE，获取商品首次采纳日期做 before-after 分析 |

### 分区写入

```
INSERT OVERWRITE ... PARTITION (tz_type = 'local', grass_region='${upper_region}', grass_date='${BIZ_YESTERDAY}')
```

- 仅写入 `tz_type = 'local'` 分区
- 每个 region 独立 pipeline 写入对应的 grass_region 分区
- 每日覆盖当天分区的全量数据

## Derived Metrics (派生指标)

无。该表是维度表，不包含可直接派生的计算指标。下游场景中派生的指标（roi2_advv = roi2_broad_gmv * target_cir 等）定义在使用方的 SQL 中。
