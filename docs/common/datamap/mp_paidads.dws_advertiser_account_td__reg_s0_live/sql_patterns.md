<!-- ads-workspace-gdoc-sync: gdoc_id=1oZe0uXZlLFfttmKay6ZBFFrg3ZJp1Ht2drG7iB5mN-8 gdoc_url=https://docs.google.com/document/d/1oZe0uXZlLFfttmKay6ZBFFrg3ZJp1Ht2drG7iB5mN-8/edit -->

# SQL Patterns: mp_paidads.dws_advertiser_account_td__reg_s0_live

*Auto-generated by ads-kb-datamap (from-code) -- 26 references (22 write, 4 read)*
*Generated on: 2026-06-16*

## Common WHERE Filters

所有引用均遵循相同的过滤模式：

```sql
-- 标准分区过滤（读/写均使用）
WHERE grass_region = upper('${region}')
  AND grass_date = DATE('${grass_date}')
```

| Filter | Pattern | Frequency | Notes |
|--------|---------|-----------|-------|
| grass_region | `= upper('${region}')` | 26/26 | 必选，按地区分区查询 |
| grass_date | `= DATE('${grass_date}')` | 4/4 (读) | 下游读取当日分区 |
| grass_date | `= date_sub(date('${grass_date}'), 1)` | 22/22 (写) | 生产时自读前一日分区 |
| tz_type | `= 'local'` | 26/26 | 固定值，所有引用均为 local 时区 |

## Common JOIN Patterns

### 下游 JOIN（ads_advertiser_mkt_1d 引用）

```sql
-- 从 ads_advertiser_mkt_1d 通过 LEFT JOIN 获取余额信息
LEFT OUTER JOIN (
    SELECT shop_id
           ,low_threshold
           ,low_threshold_usd
           ,is_reach_threshold
           ,grass_region
    FROM mp_paidads.dws_advertiser_account_td__reg_s0_live
    WHERE grass_region = upper('${region}')
      AND grass_date = DATE('${grass_date}')
) balance
ON dim.shop_id = balance.shop_id
   AND dim.grass_region = balance.grass_region
```

**JOIN 键**: `shop_id + grass_region`

### 自 JOIN（生产逻辑）

```sql
-- FULL OUTER JOIN 前日 to-date 记录，COALESCE 优先取当日值
SELECT COALESCE(a.shop_id, b.shop_id) AS shop_id
      ,COALESCE(a.acc_before_balance, b.acc_before_balance) AS acc_before_balance
      ...
FROM (SELECT ... FROM mp_paidads.dws_advertiser_account_1d__reg_s0_live
      WHERE grass_date = date('${grass_date}')) a
FULL OUTER JOIN (
    SELECT ... FROM mp_paidads.dws_advertiser_account_td__reg_s0_live
    WHERE grass_date = date_sub(date('${grass_date}'), 1)
) b
ON a.shop_id = b.shop_id AND a.grass_region = b.grass_region
```

**JOIN 键**: `shop_id + grass_region`（自 JOIN）

## Common Aggregations

该表为 to-date 快照表，下游使用时**不做聚合**，仅提取字段值：

- 余额提取: `balance.low_threshold`, `balance.low_threshold_usd`
- 状态提取: `COALESCE(balance.is_reach_threshold, 0) AS is_reach_low_threshold`

无 GROUP BY 聚合模式。

## Representative SQL Snippets

### 1. 下游消费 — ads_advertiser_mkt_1d 报表

```sql
cache table balance OPTIONS ('storageLevel' 'MEMORY_AND_DISK')
select  shop_id
        ,low_threshold
        ,low_threshold_usd
        ,is_reach_threshold
        ,grass_region
from    mp_paidads.dws_advertiser_account_td__reg_s0_live
where   grass_region = upper('${region}')
  and   grass_date = DATE('${grass_date}');
```

### 2. 生产写入 — 非US区域 (id, sg, ph, my, th, tw, vn, mx, co, cl, br)

```sql
INSERT OVERWRITE TABLE dws_advertiser_account_td__reg_s0_live
    PARTITION (tz_type='local', grass_region, grass_date)
SELECT
    COALESCE(a.user_id, b.user_id) AS user_id
   ,COALESCE(a.shop_id, b.shop_id) AS shop_id
   ,COALESCE(a.acc_before_balance, b.acc_before_balance) AS acc_before_balance
   ,COALESCE(a.acc_before_balance_usd, b.acc_before_balance_usd) AS acc_before_balance_usd
   ,COALESCE(a.acc_after_balance, b.acc_after_balance) AS acc_after_balance
   ,COALESCE(a.acc_after_balance_usd, b.acc_after_balance_usd) AS acc_after_balance_usd
   ,COALESCE(a.low_threshold, b.low_threshold) AS low_threshold
   ,COALESCE(a.low_threshold_usd, b.low_threshold_usd) AS low_threshold_usd
   ,COALESCE(a.is_reach_threshold, b.is_reach_threshold) AS is_reach_threshold
   ,COALESCE(a.grass_region, b.grass_region) AS grass_region
   ,date('${grass_date}') AS grass_date
FROM (
    SELECT * FROM mp_paidads.dws_advertiser_account_1d__reg_s0_live
    WHERE grass_region = upper('${region}')
      AND grass_date = date('${grass_date}')
) a
FULL OUTER JOIN (
    SELECT * FROM mp_paidads.dws_advertiser_account_td__reg_s0_live
    WHERE grass_region = upper('${region}')
      AND grass_date = date_sub(date('${grass_date}'), 1)
) b
ON a.shop_id = b.shop_id AND a.grass_region = b.grass_region;
```

### 3. 生产写入 — US区域 (mx, co, cl, br US)

```sql
-- US区域在写入前需先 CREATE TABLE (含 DDL)
create table if not exists dws_advertiser_account_td__${region}_s0_live
(
    user_id BIGINT comment 'userid'
   ,shop_id BIGINT comment 'shopid'
   ,acc_before_balance DOUBLE comment 'the balance remains when acc before in local currency'
   ,acc_before_balance_usd DOUBLE comment 'the balance remains when acc before in usd currency'
   ,acc_after_balance DOUBLE comment 'the balance remains when acc after in local currency'
   ,acc_after_balance_usd DOUBLE comment 'the balance remains when acc after in usd currency'
   ,low_threshold DOUBLE comment 'low threshold in local'
   ,low_threshold_usd DOUBLE comment 'low threshold in usd'
   ,is_reach_threshold TINYINT comment 'ever reached today then will be set to 1 else 0'
   ,grass_date DATE
) using parquet
partitioned by (grass_date)
comment 'aggregated data based on the processed data from ads tracking'
location '${HIVE_PATH}/dws_advertiser_account_td/tz_type=local/grass_region=${upper_region}';

INSERT OVERWRITE TABLE dws_advertiser_account_td__reg_s0_live
    PARTITION (tz_type='local', grass_region, grass_date)
-- (same SELECT as non-US pattern)
...
```

## Write Lineage (生产血缘)

### 上游表

| 上游表 | JOIN 方式 | 频率 |
|--------|-----------|------|
| `mp_paidads.dws_advertiser_account_1d__reg_s0_live` | FULL OUTER JOIN (当日快照) | 22/22 |
| `mp_paidads.dws_advertiser_account_td__reg_s0_live` | FULL OUTER JOIN (前日 to-date) | 22/22 (自读) |

### 写入策略

- **方式**: INSERT OVERWRITE PARTITION
- **分区**: `tz_type='local', grass_region, grass_date`
- **逻辑**: 每日每个 region 运行一个独立 workflow task，COALESCE 合并当日快照与前日 to-date 记录
- **区域**: 共 22 个 region task（含 US 和非 US）

| 区域类型 | 路径 | Task 数量 |
|----------|------|-----------|
| Non-US regions | `workflows/data_warehouse/dws/revenue/dws_advertiser_account_td__reg_s0_live/Data Processing/` | 18 |
| US regions | `workflows/data_warehouse_us/dws/dws_advertiser_account_td__reg_s0_live/Data Processing/` | 4 |

### 下游表

| 下游表 | 读取字段 | JOIN 键 |
|--------|----------|---------|
| `mp_paidads.ads_advertiser_mkt_1d__reg_s0_live` | low_threshold, low_threshold_usd, is_reach_threshold | shop_id + grass_region |

## Derived Metrics (派生指标)

该表为原始快照表，代码库中未发现派生指标。所有字段均为上游 `dws_advertiser_account_1d__reg_s0_live` 的直接引用。
