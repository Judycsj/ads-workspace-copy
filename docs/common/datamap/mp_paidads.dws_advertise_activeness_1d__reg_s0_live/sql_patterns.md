<!-- ads-workspace-gdoc-sync: gdoc_id=13OkaowHmaoicjPLgImrLHDkffcdbtqFQckW3Yw6Y9iE gdoc_url=https://docs.google.com/document/d/13OkaowHmaoicjPLgImrLHDkffcdbtqFQckW3Yw6Y9iE/edit -->

# SQL Patterns: mp_paidads.dws_advertise_activeness_1d__reg_s0_live

*Auto-generated by ads-kb-datamap (from-code) -- 19 references (19 write, 0 read)*
*Generated on: 2026-06-16*

## Common WHERE Filters

### Partition Filter

```sql
-- 标准分区过滤
WHERE grass_date = date('${grass_date}')
  AND is_ads_active = 1
```

### 业务过滤

- `is_ads_active = 1` — 仅统计激活状态的广告
- `placement = 0` — 过滤 MM Keyword 广告激活日期
- `placement = 4` — 过滤 SM Keyword 广告激活日期

## Common JOIN Patterns

本表为 DWS 汇总层，生产过程中只从上游表 `mp_paidads.dim_advertise` 读取：

```sql
FROM mp_paidads.dim_advertise__${region}_s0_live
```

无其他 JOIN 模式。

## Common Aggregations

```sql
-- 以 ads_id + shop_id + user_id 为最细粒度取首次激活日期
SELECT
    ads_id
    ,shop_id
    ,seller_id AS user_id
    ,MIN(first_mm_kw_ads_active_date) AS first_mm_kw_ads_active_date
    ,MIN(first_sm_kw_ads_active_date) AS first_sm_kw_ads_active_date
    ,upper('${region}') AS grass_region
    ,date('${grass_date}') AS grass_date
FROM (...)
GROUP BY ads_id, shop_id, seller_id
```

## Representative SQL Snippets

### Snippet 1: 完整 INSERT 语句 (生产逻辑)

```sql
INSERT OVERWRITE TABLE dws_advertise_activeness_1d__reg_s0_live
PARTITION (tz_type='local', grass_region, grass_date)
SELECT
    ads_id
    ,shop_id
    ,seller_id AS user_id
    ,MIN(first_mm_kw_ads_active_date) AS first_mm_kw_ads_active_date
    ,MIN(first_sm_kw_ads_active_date) AS first_sm_kw_ads_active_date
    ,upper('${region}') AS grass_region
    ,date('${grass_date}') AS grass_date
FROM (
    SELECT
        ads_id
        ,shop_id
        ,seller_id
        ,CASE WHEN placement = 0 THEN grass_date END AS first_mm_kw_ads_active_date
        ,CASE WHEN placement = 4 THEN grass_date END AS first_sm_kw_ads_active_date
        ,CASE WHEN placement = 1000 THEN grass_date END AS first_boost_kw_ads_active_date
    FROM mp_paidads.dim_advertise__${region}_s0_live
    WHERE grass_date = date('${grass_date}')
      AND is_ads_active = 1
)
GROUP BY ads_id, shop_id, seller_id
```

### Snippet 2: 查询某地区广告激活日期

```sql
-- 查询指定地区广告的首次关键词激活日期
SELECT
    ads_id
    ,shop_id
    ,user_id
    ,first_mm_kw_ads_active_date
    ,first_sm_kw_ads_active_date
FROM mp_paidads.dws_advertise_activeness_1d__reg_s0_live
WHERE grass_region = 'ID'
  AND grass_date = '2026-06-15'
  AND tz_type = 'local'
```

## Write Lineage (生产血缘)

```
mp_paidads.dim_advertise__${region}_s0_live
    │  WHERE grass_date = '${date}' AND is_ads_active = 1
    │  CASE-WHEN placement → first_mm/sm_kw_ads_active_date
    │  GROUP BY ads_id, shop_id, seller_id
    │  AGG: MIN(first_*_date)
    ▼
mp_paidads.dws_advertise_activeness_1d__reg_s0_live
    PARTITION (tz_type='local', grass_region, grass_date)

生产路径:
  workflows/data_warehouse/dws/setup/dws_advertise_activeness_1d__reg_s0_live/ (15 files, 11 regions)
  workflows/data_warehouse_us/dws/dws_advertise_activeness_1d__reg_s0_live/ (4 files, US regions)
```

## Derived Metrics (派生指标)

| 派生字段 | 公式 | 说明 |
|----------|------|------|
| first_mm_kw_ads_active_date | `MIN(CASE WHEN placement = 0 THEN grass_date END)` | 首次 MM 关键词广告激活日期 |
| first_sm_kw_ads_active_date | `MIN(CASE WHEN placement = 4 THEN grass_date END)` | 首次 SM 关键词广告激活日期 |
| user_id | `seller_id` (alias) | 广告主 user_id，即为 seller_id |

## Notes

- 表仅包含 `tz_type='local'` 的数据，无 UTC 时区分区
- 上游 dim_advertise 中 `placement=1000`（Boost）的 CASE WHEN 逻辑存在但未输出到最终表
- 每次 INSERT 同时写两个目标：`dws_advertise_activeness_1d__reg_s0_live` (union) 和 `dws_advertise_activeness_1d__${region}_s0_live` (单地区)
- 所有生产任务均在 `workflows/` 路径下，为正式调度任务
- 未在代码库中发现读引用（下游消费可能通过 DataStudio 或其他未纳入版本控制的查询执行）
