# {Contract Name} 契约/{Contract Name} Contract

> {一句话定位：定义 X 的稳定字段契约，供 A、B、C 引用}

**权威依赖**：核心概念见 [`02-{foundational}.md`](./02-{foundational}.md)。

---

本文件定义 {data structure/interface} 的稳定字段契约，供以下组件/技能引用：

- `{consumer-1}`：{how it uses this contract}
- `{consumer-2}`：{how it uses this contract}

## 总体要求/General Requirements

{描述总体约束和原则}

## {Section} {Field Group Name}

| 列名/字段 | 含义 | 允许值 |
| --- | --- | --- |
| {field-1} | {meaning} | {allowed values} |
| {field-2} | {meaning} | {allowed values} |

**规则/Rules**：

- {rule 1}
- {rule 2}

<!-- 按需添加更多 section -->

## {Section} {Another Field Group}

| 列名/字段 | 含义 | 允许值 |
| --- | --- | --- |
| {field-1} | {meaning} | {allowed values} |

## 变更护栏/Change Guardrails

| 修改内容 | 需同步 |
| --- | --- |
| {field change} | {consumers that must be updated} |
